# Before / After

## Before

- Runtime APIs documented separately
- Permissions documented separately
- Deployment documented separately
- Rendering limitations documented separately

Developers reconstruct operational behavior manually.

---

## After

```text
target
→ runtime
→ deployment state
→ permissions
→ rendering constraints
→ available APIs
→ expected behavior
```
