extends: existence
message: "Behavior model vocabulary detected: verify that condition/state/action/outcome are explicit."
level: suggestion
ignorecase: true

tokens:
  - behavior model
  - dependency model
  - lifecycle model
  - state transition
  - runtime behavior
  - approval workflow
  - permission scope
  - retry behavior
  - error path
  - synchronization behavior
